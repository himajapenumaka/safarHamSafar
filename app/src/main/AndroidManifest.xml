<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ks.safarhamsafar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectPaymentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NetBankingActivity"></activity>
        <activity android:name=".PayPalActivity" />
        <activity android:name=".EmiBankActivity" />
        <activity android:name=".GiftCardActivity" />
        <activity android:name=".PayByUpiActivity" />
        <activity android:name=".MobileWalletActivity" />
        <activity android:name=".CardInfoActivity" />
        <activity android:name=".AmazonPayActivity" />
        <activity android:name=".LogIn.LoginActivity2" />
        <activity android:name=".LogIn.LoginActivity1" />
        <activity android:name=".LogIn.LoginActivity3" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>